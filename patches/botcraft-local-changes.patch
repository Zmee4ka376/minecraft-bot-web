From df2a4031cf1fa1e5f66ce9942adc120984a3533f Mon Sep 17 00:00:00 2001
From: Zmee4ka376 <zmee4ka376@users.noreply.github.com>
Date: Fri, 21 Aug 2026 01:56:45 +0300
Subject: [PATCH 1/3] Fix LocalPlayer shared_ptr data races (crash 0xc0000005
 during login) + server compat changes

---
 botcraft/CMakeLists.txt                       |   1 +
 .../botcraft/Game/Physics/PhysicsManager.hpp  |   1 +
 .../botcraft/Network/NetworkManager.hpp       |  25 +-
 .../botcraft/Network/TCP_Com.hpp              |   3 +
 botcraft/src/Game/Entities/EntityManager.cpp  |  53 +++-
 .../src/Game/Inventory/InventoryManager.cpp   |  15 +
 botcraft/src/Game/Physics/PhysicsManager.cpp  |  40 ++-
 botcraft/src/Game/World/Chunk.cpp             |  17 +-
 botcraft/src/Game/World/World.cpp             |   6 +
 botcraft/src/Network/NetworkManager.cpp       | 272 +++++++++++++++---
 botcraft/src/Network/TCP_Com.cpp              |  13 +
 protocolCraft/CMakeLists.txt                  |   1 +
 12 files changed, 373 insertions(+), 74 deletions(-)

diff --git a/botcraft/CMakeLists.txt b/botcraft/CMakeLists.txt
index 617cfec..58dce96 100644
--- a/botcraft/CMakeLists.txt
+++ b/botcraft/CMakeLists.txt
@@ -616,6 +616,7 @@ endif(MSVC)
 if (MSVC)
     target_compile_options(botcraft PRIVATE "/bigobj")
     set_target_properties(botcraft PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded")
+    target_compile_definitions(botcraft PRIVATE _ITERATOR_DEBUG_LEVEL=0)
 endif (MSVC)
 
 # Set version header
diff --git a/botcraft/include/botcraft/Game/Physics/PhysicsManager.hpp b/botcraft/include/botcraft/Game/Physics/PhysicsManager.hpp
index 0994200..4fa7c68 100644
--- a/botcraft/include/botcraft/Game/Physics/PhysicsManager.hpp
+++ b/botcraft/include/botcraft/Game/Physics/PhysicsManager.hpp
@@ -105,6 +105,7 @@ namespace Botcraft
         std::shared_ptr<EntityManager> entity_manager;
         std::shared_ptr<InventoryManager> inventory_manager;
         std::shared_ptr<LocalPlayer> player;
+        mutable std::shared_mutex player_mutex;
         std::shared_ptr<NetworkManager> network_manager;
         std::shared_ptr<World> world;
 
diff --git a/botcraft/include/botcraft/Network/NetworkManager.hpp b/botcraft/include/botcraft/Network/NetworkManager.hpp
index 5d0ac01..52c30f0 100644
--- a/botcraft/include/botcraft/Network/NetworkManager.hpp
+++ b/botcraft/include/botcraft/Network/NetworkManager.hpp
@@ -12,9 +12,7 @@
 #if PROTOCOL_VERSION > 759 /* > 1.19 */
 #include "botcraft/Network/LastSeenMessagesTracker.hpp"
 #endif
-#if PROTOCOL_VERSION > 760 /* > 1.19.2 */
 #include <atomic>
-#endif
 
 namespace Botcraft
 {
@@ -37,6 +35,9 @@ namespace Botcraft
         ~NetworkManager();
 
         void Stop();
+        // Stop network I/O immediately without joining the processing thread.
+        // This is safe to call from a packet handler running on that thread.
+        void Abort();
 
         void AddHandler(ProtocolCraft::Handler* h);
         void Send(const std::shared_ptr<ProtocolCraft::Packet> packet);
@@ -52,6 +53,7 @@ namespace Botcraft
         void WaitForNewPackets();
         void ProcessPacket(const std::vector<unsigned char>& bytes);
         void OnNewRawData(const std::vector<unsigned char>& bytes);
+        void SetState(const ProtocolCraft::ConnectionState new_state);
 
 
         virtual void Handle(ProtocolCraft::ClientboundLoginCompressionPacket& packet) override;
@@ -82,6 +84,16 @@ namespace Botcraft
         virtual void Handle(ProtocolCraft::ClientboundStartConfigurationPacket& packet) override;
         virtual void Handle(ProtocolCraft::ClientboundChunkBatchStartPacket& packet) override;
         virtual void Handle(ProtocolCraft::ClientboundChunkBatchFinishedPacket& packet) override;
+#if PROTOCOL_VERSION == 764 /* 1.20.2 */
+        virtual void Handle(ProtocolCraft::ClientboundResourcePackConfigurationPacket& packet) override;
+#else
+        virtual void Handle(ProtocolCraft::ClientboundResourcePackPushConfigurationPacket& packet) override;
+#endif
+#endif
+#if PROTOCOL_VERSION < 765 /* < 1.20.3 */
+        virtual void Handle(ProtocolCraft::ClientboundResourcePackPacket& packet) override;
+#else
+        virtual void Handle(ProtocolCraft::ClientboundResourcePackPushPacket& packet) override;
 #endif
 #if PROTOCOL_VERSION > 765 /* > 1.20.4 */
         virtual void Handle(ProtocolCraft::ClientboundSelectKnownPacksPacket& packet) override;
@@ -90,12 +102,19 @@ namespace Botcraft
         virtual void Handle(ProtocolCraft::ClientboundPlayerAbilitiesPacket& packet) override;
 #endif
 
+#if PROTOCOL_VERSION > 763 /* > 1.20.1 */
+        // Send the client brand and client information packets. Used both during
+        // the initial login configuration and when re-entering configuration
+        // (server transfer).
+        void SendBrandAndClientInformation();
+#endif
+
     private:
         std::vector<ProtocolCraft::Handler*> subscribed;
 
         std::shared_ptr<TCP_Com> com;
         std::shared_ptr<Authentifier> authentifier;
-        ProtocolCraft::ConnectionState state;
+        std::atomic<ProtocolCraft::ConnectionState> state;
 
         std::thread m_thread_process;//Thread running to process incoming packets without blocking com
 
diff --git a/botcraft/private_include/botcraft/Network/TCP_Com.hpp b/botcraft/private_include/botcraft/Network/TCP_Com.hpp
index 9b12bd4..c85962d 100644
--- a/botcraft/private_include/botcraft/Network/TCP_Com.hpp
+++ b/botcraft/private_include/botcraft/Network/TCP_Com.hpp
@@ -30,6 +30,8 @@ namespace Botcraft
         void SetEncrypter(const std::shared_ptr<AESEncrypter> encrypter_);
 #endif
 
+        void SetOnDisconnect(std::function<void()> callback);
+
         const std::string& GetIp() const;
         const unsigned short GetPort() const;
 
@@ -60,6 +62,7 @@ namespace Botcraft
         std::deque<std::vector<unsigned char> > output_packet;
 
         std::function<void(const std::vector<unsigned char>&)> NewPacketCallback;
+        std::function<void()> OnDisconnect;
         std::mutex mutex_output;
 
         std::string ip;
diff --git a/botcraft/src/Game/Entities/EntityManager.cpp b/botcraft/src/Game/Entities/EntityManager.cpp
index e566e31..dae646b 100644
--- a/botcraft/src/Game/Entities/EntityManager.cpp
+++ b/botcraft/src/Game/Entities/EntityManager.cpp
@@ -15,6 +15,7 @@ namespace Botcraft
 
     std::shared_ptr<LocalPlayer> EntityManager::GetLocalPlayer()
     {
+        std::shared_lock<std::shared_mutex> lock(entity_manager_mutex);
         return local_player;
     }
 
@@ -44,14 +45,15 @@ namespace Botcraft
 
     void EntityManager::Handle(ProtocolCraft::ClientboundLoginPacket& packet)
     {
-        local_player = std::make_shared<LocalPlayer>();
-        local_player->SetEntityID(packet.GetPlayerId());
+        std::shared_ptr<LocalPlayer> new_player = std::make_shared<LocalPlayer>();
+        new_player->SetEntityID(packet.GetPlayerId());
 #if PROTOCOL_VERSION < 764 /* < 1.20.2 */
-        local_player->SetGameMode(static_cast<GameType>(packet.GetGameType() & 0x03));
+        new_player->SetGameMode(static_cast<GameType>(packet.GetGameType() & 0x03));
 #else
-        local_player->SetGameMode(static_cast<GameType>(packet.GetCommonPlayerSpawnInfo().GetGameType()));
+        new_player->SetGameMode(static_cast<GameType>(packet.GetCommonPlayerSpawnInfo().GetGameType()));
 #endif
         std::scoped_lock<std::shared_mutex> lock(entity_manager_mutex);
+        local_player = new_player;
         entities[packet.GetPlayerId()] = local_player;
     }
 
@@ -171,7 +173,7 @@ namespace Botcraft
     void EntityManager::Handle(ProtocolCraft::ClientboundExplodePacket& packet)
     {
 
-        std::shared_ptr<Entity> entity = local_player;
+        std::shared_ptr<Entity> entity = GetLocalPlayer();
 
         if (entity == nullptr)
         {
@@ -275,9 +277,14 @@ namespace Botcraft
 
     void EntityManager::Handle(ProtocolCraft::ClientboundSetHealthPacket& packet)
     {
-        local_player->SetHealth(packet.GetHealth());
-        local_player->SetFood(packet.GetFood());
-        local_player->SetFoodSaturation(packet.GetFoodSaturation());
+        std::shared_ptr<LocalPlayer> player = GetLocalPlayer();
+        if (player == nullptr)
+        {
+            return;
+        }
+        player->SetHealth(packet.GetHealth());
+        player->SetFood(packet.GetFood());
+        player->SetFoodSaturation(packet.GetFoodSaturation());
     }
 
     void EntityManager::Handle(ProtocolCraft::ClientboundTeleportEntityPacket& packet)
@@ -318,9 +325,14 @@ namespace Botcraft
 
     void EntityManager::Handle(ProtocolCraft::ClientboundPlayerAbilitiesPacket& packet)
     {
-        local_player->SetAbilitiesFlags(packet.GetFlags());
-        local_player->SetFlyingSpeed(packet.GetFlyingSpeed());
-        local_player->SetWalkingSpeed(packet.GetWalkingSpeed());
+        std::shared_ptr<LocalPlayer> player = GetLocalPlayer();
+        if (player == nullptr)
+        {
+            return;
+        }
+        player->SetAbilitiesFlags(packet.GetFlags());
+        player->SetFlyingSpeed(packet.GetFlyingSpeed());
+        player->SetWalkingSpeed(packet.GetWalkingSpeed());
     }
 
 #if PROTOCOL_VERSION == 755 /* 1.17 */
@@ -342,10 +354,15 @@ namespace Botcraft
 
     void EntityManager::Handle(ProtocolCraft::ClientboundRespawnPacket& packet)
     {
+        std::shared_ptr<LocalPlayer> player = GetLocalPlayer();
+        if (player == nullptr)
+        {
+            return;
+        }
 #if PROTOCOL_VERSION < 764 /* < 1.20.2 */
-        local_player->SetGameMode(static_cast<GameType>(packet.GetPlayerGameType()));
+        player->SetGameMode(static_cast<GameType>(packet.GetPlayerGameType()));
 #else
-        local_player->SetGameMode(static_cast<GameType>(packet.GetCommonPlayerSpawnInfo().GetGameType()));
+        player->SetGameMode(static_cast<GameType>(packet.GetCommonPlayerSpawnInfo().GetGameType()));
 #endif
     }
 
@@ -354,8 +371,14 @@ namespace Botcraft
         switch (packet.GetType())
         {
         case 3: // CHANGE_GAME_MODE
-            local_player->SetGameMode(static_cast<GameType>(packet.GetParam()));
+        {
+            std::shared_ptr<LocalPlayer> player = GetLocalPlayer();
+            if (player != nullptr)
+            {
+                player->SetGameMode(static_cast<GameType>(packet.GetParam()));
+            }
             break;
+        }
         default:
             break;
         }
@@ -560,7 +583,7 @@ namespace Botcraft
         if (entity != nullptr)
         {
             entity->SetPosition(packet.GetValues().GetPosition());
-            if (entity == local_player)
+            if (entity == GetLocalPlayer())
             {
                 return;
             }
diff --git a/botcraft/src/Game/Inventory/InventoryManager.cpp b/botcraft/src/Game/Inventory/InventoryManager.cpp
index b35f052..492ae2e 100644
--- a/botcraft/src/Game/Inventory/InventoryManager.cpp
+++ b/botcraft/src/Game/Inventory/InventoryManager.cpp
@@ -517,6 +517,7 @@ namespace Botcraft
 #if PROTOCOL_VERSION < 755 /* < 1.17 */
         AddPendingTransaction(inventory_transaction);
         network_manager->Send(transaction);
+        ApplyTransaction(inventory_transaction);
         return transaction->GetUid();
 #else
         network_manager->Send(transaction);
@@ -596,6 +597,20 @@ namespace Botcraft
 
     void InventoryManager::Handle(ClientboundOpenScreenPacket& packet)
     {
+        // Close any previously open non-player containers before opening the new one.
+        // Some servers (e.g. mc.reallyworld.ru) don't send ClientboundContainerClosePacket
+        // before a new OpenScreen, so we must clean up here to avoid stale windows.
+        {
+            std::scoped_lock<std::shared_mutex> lock(inventory_manager_mutex);
+            for (auto it = inventories.begin(); it != inventories.end(); )
+            {
+                if (it->first != Window::PLAYER_INVENTORY_INDEX)
+                    it = inventories.erase(it);
+                else
+                    ++it;
+            }
+        }
+
 #if PROTOCOL_VERSION < 452 /* < 1.14 */
         InventoryType type = InventoryType::Default;
         if (packet.GetType() == "minecraft:chest")
diff --git a/botcraft/src/Game/Physics/PhysicsManager.cpp b/botcraft/src/Game/Physics/PhysicsManager.cpp
index 3431f85..5ca5748 100644
--- a/botcraft/src/Game/Physics/PhysicsManager.cpp
+++ b/botcraft/src/Game/Physics/PhysicsManager.cpp
@@ -76,6 +76,11 @@ namespace Botcraft
     {
         double_tap_cause_sprint = b;
         // Reset sprint_double_tap_trigger_time to 0 if b is false
+        std::shared_ptr<LocalPlayer> player;
+        {
+            std::shared_lock<std::shared_mutex> lock(player_mutex);
+            player = this->player;
+        }
         if (player != nullptr)
         {
             std::scoped_lock<std::shared_mutex> lock(player->entity_mutex);
@@ -93,21 +98,29 @@ namespace Botcraft
     {
         // Reset the player because *some* non vanilla servers
         // sends new login packets
-        if (player != nullptr)
+        const std::shared_ptr<LocalPlayer> new_player = entity_manager->GetLocalPlayer();
+        std::shared_ptr<LocalPlayer> old_player;
         {
-            // If we already have a player, we need to make sure we're not in the middle
-            // of a physics update while swapping the player with the new one
-            std::scoped_lock<std::shared_mutex> lock(player->entity_mutex);
-            player = entity_manager->GetLocalPlayer();
+            std::scoped_lock<std::shared_mutex> lock(player_mutex);
+            old_player = player;
+            player = new_player;
         }
-        else
+        if (old_player != nullptr)
         {
-            player = entity_manager->GetLocalPlayer();
+            // If we already have a player, we need to make sure we're not in the middle
+            // of a physics update while swapping the player with the new one
+            std::scoped_lock<std::shared_mutex> lock(old_player->entity_mutex);
         }
     }
 
     void PhysicsManager::Handle(ClientboundPlayerPositionPacket& packet)
     {
+        std::shared_ptr<LocalPlayer> player;
+        {
+            std::shared_lock<std::shared_mutex> lock(player_mutex);
+            player = this->player;
+        }
+
         if (player == nullptr)
         {
             LOG_WARNING("Received a PlayerPosition packet without a player");
@@ -215,6 +228,12 @@ namespace Botcraft
 #if PROTOCOL_VERSION > 767 /* > 1.21.1 */
     void PhysicsManager::Handle(ClientboundPlayerRotationPacket& packet)
     {
+        std::shared_ptr<LocalPlayer> player;
+        {
+            std::shared_lock<std::shared_mutex> lock(player_mutex);
+            player = this->player;
+        }
+
         if (player == nullptr)
         {
             LOG_WARNING("Received a PlayerRotation packet without a player");
@@ -246,6 +265,9 @@ namespace Botcraft
 
             if (network_manager->GetConnectionState() == ConnectionState::Play)
             {
+                // Hold a read lock on the player pointer for the whole tick, so a
+                // login packet can't swap it in the middle of a physics update
+                std::shared_lock<std::shared_mutex> player_lock(player_mutex);
                 if (player != nullptr && !std::isnan(player->GetY()))
                 {
                     // As PhysicsManager is a friend of LocalPlayer, we can lock the whole entity
@@ -1304,8 +1326,8 @@ namespace Botcraft
         // Move with elytra
         else if (player->GetDataSharedFlagsIdImpl(EntitySharedFlagsId::FallFlying))
         {
-            // sqrt(front_vector.x� + front_vector.z�) to follow vanilla code
-            // it's equal to cos(pitch) (as -90�<=pitch<=90�, cos(pitch) >= 0.0)
+            // sqrt(front_vector.x� + front_vector.z�) to follow vanilla code
+            // it's equal to cos(pitch) (as -90�<=pitch<=90�, cos(pitch) >= 0.0)
             const double cos_pitch_from_length = std::sqrt(player->front_vector.x * player->front_vector.x + player->front_vector.z * player->front_vector.z);
             const double cos_pitch = std::cos(static_cast<double>(player->pitch * 0.017453292f /* PI/180 */));
             const double cos_pitch_sqr = cos_pitch * cos_pitch;
diff --git a/botcraft/src/Game/World/Chunk.cpp b/botcraft/src/Game/World/Chunk.cpp
index f038522..85acf2e 100644
--- a/botcraft/src/Game/World/Chunk.cpp
+++ b/botcraft/src/Game/World/Chunk.cpp
@@ -115,7 +115,22 @@ namespace Botcraft
 
         if (data.size() == 0)
         {
-            LOG_ERROR("Cannot load chunk data without data");
+            // An empty data array is legitimate when no section is set
+            // (e.g. void/superflat hub worlds). Only warn if some section
+            // was expected, as that means the packet was not parsed correctly.
+#if PROTOCOL_VERSION < 755 /* < 1.17 */
+            if (primary_bit_mask != 0)
+#else
+            bool has_section = false;
+            for (const auto& m : primary_bit_mask)
+            {
+                if (m != 0) { has_section = true; break; }
+            }
+            if (has_section)
+#endif
+            {
+                LOG_ERROR("Cannot load chunk data without data");
+            }
             return;
         }
 
diff --git a/botcraft/src/Game/World/World.cpp b/botcraft/src/Game/World/World.cpp
index 65341d4..3fec0c2 100644
--- a/botcraft/src/Game/World/World.cpp
+++ b/botcraft/src/Game/World/World.cpp
@@ -718,12 +718,15 @@ namespace Botcraft
 
 #if PROTOCOL_VERSION > 718 /* > 1.15.2 */
 #if PROTOCOL_VERSION < 751 /* < 1.16.2 */
+        try {
         for (const auto& d : packet.GetRegistryHolder()["dimension"].as_list_of<ProtocolCraft::NBT::TagCompound>())
         {
             const std::string& dim_name = d["name"].get<std::string>();
             dimension_fast_lava[dim_name] = static_cast<bool>(d["ultrawarm"].get<char>());
         }
+        } catch (...) { LOG_WARNING("Failed to parse dimension registry (pre-1.16.2)"); }
 #elif PROTOCOL_VERSION < 764 /* < 1.20.2 */
+        try {
         for (const auto& d : packet.GetRegistryHolder()["minecraft:dimension_type"]["value"].as_list_of<ProtocolCraft::NBT::TagCompound>())
         {
             const std::string& dim_name = d["name"].get<std::string>();
@@ -733,6 +736,7 @@ namespace Botcraft
             dimension_min_y[dim_name] = d["element"]["min_y"].get<int>();
 #endif
         }
+        } catch (...) { LOG_WARNING("Failed to parse dimension registry (1.16.2+)"); }
 #endif
 #endif
     }
@@ -751,11 +755,13 @@ namespace Botcraft
 #endif
 
 #if PROTOCOL_VERSION > 747 /* > 1.16.1 */ && PROTOCOL_VERSION < 759 /* < 1.19 */
+        try {
         dimension_fast_lava[current_dimension] = static_cast<bool>(packet.GetDimensionType()["ultrawarm"].get<char>());
 #if PROTOCOL_VERSION > 756 /* > 1.17.1 */
         dimension_height[current_dimension] = packet.GetDimensionType()["height"].get<int>();
         dimension_min_y[current_dimension] = packet.GetDimensionType()["min_y"].get<int>();
 #endif
+        } catch (...) { LOG_WARNING("Failed to parse dimension type on respawn"); }
 #endif
     }
 
diff --git a/botcraft/src/Network/NetworkManager.cpp b/botcraft/src/Network/NetworkManager.cpp
index 6c5c487..1d2ecc9 100644
--- a/botcraft/src/Network/NetworkManager.cpp
+++ b/botcraft/src/Network/NetworkManager.cpp
@@ -23,10 +23,41 @@ using namespace ProtocolCraft;
 
 namespace Botcraft
 {
+    namespace
+    {
+        std::string StateToString(const ConnectionState state)
+        {
+            switch (state)
+            {
+            case ConnectionState::None: return "None";
+            case ConnectionState::Handshake: return "Handshake";
+            case ConnectionState::Status: return "Status";
+            case ConnectionState::Login: return "Login";
+            case ConnectionState::Play: return "Play";
+#if PROTOCOL_VERSION > 763 /* > 1.20.1 */
+            case ConnectionState::Configuration: return "Configuration";
+#endif
+            default: return "Unknown";
+            }
+        }
+    }
+
+    void NetworkManager::SetState(const ProtocolCraft::ConnectionState new_state)
+    {
+        const ProtocolCraft::ConnectionState old_state = state.load();
+        if (old_state != new_state)
+        {
+            LOG_INFO("[STATE] " << StateToString(old_state) << " -> " << StateToString(new_state));
+        }
+        state = new_state;
+    }
+
     NetworkManager::NetworkManager(const std::string& address, const std::string& login_or_microsoft_cache_key_or_mc_token, const AuthType auth_type, const std::vector<Handler*>& handlers)
     {
         com = nullptr;
 
+        LOG_INFO("[NETWORK] Creating NetworkManager for " << address);
+
         switch (auth_type)
         {
         case AuthType::Offline:
@@ -58,50 +89,66 @@ namespace Botcraft
             AddHandler(p);
         }
 
-        state = ConnectionState::Handshake;
+        SetState(ConnectionState::Handshake);
 
-        //Start the thread to process the incoming packets
-        m_thread_process = std::thread(&NetworkManager::WaitForNewPackets, this);
+        try
+        {
+            //Start the thread to process the incoming packets
+            m_thread_process = std::thread(&NetworkManager::WaitForNewPackets, this);
 
-        com = std::make_shared<TCP_Com>(address, std::bind(&NetworkManager::OnNewRawData, this, std::placeholders::_1));
+            com = std::make_shared<TCP_Com>(address, std::bind(&NetworkManager::OnNewRawData, this, std::placeholders::_1));
 
-        // Wait for the communication to be ready before sending any data
-        Utilities::WaitForCondition([&]() {
-            return com->IsInitialized();
-        }, 500, 0);
+            // Wait for the communication to be ready before sending any data
+            Utilities::WaitForCondition([&]() {
+                return com->IsInitialized();
+            }, 500, 0);
 
-        std::shared_ptr<ServerboundClientIntentionPacket> handshake_packet = std::make_shared<ServerboundClientIntentionPacket>();
-        handshake_packet->SetProtocolVersion(PROTOCOL_VERSION);
-        handshake_packet->SetHostName(com->GetIp());
-        handshake_packet->SetPort(com->GetPort());
-        handshake_packet->SetIntention(static_cast<int>(ConnectionState::Login));
-        Send(handshake_packet);
+            std::shared_ptr<ServerboundClientIntentionPacket> handshake_packet = std::make_shared<ServerboundClientIntentionPacket>();
+            handshake_packet->SetProtocolVersion(PROTOCOL_VERSION);
+            handshake_packet->SetHostName(com->GetIp());
+            handshake_packet->SetPort(com->GetPort());
+            handshake_packet->SetIntention(static_cast<int>(ConnectionState::Login));
+            Send(handshake_packet);
 
-        state = ConnectionState::Login;
+            SetState(ConnectionState::Login);
 
-        std::shared_ptr<ServerboundHelloPacket> loginstart_packet = std::make_shared<ServerboundHelloPacket>();
+            std::shared_ptr<ServerboundHelloPacket> loginstart_packet = std::make_shared<ServerboundHelloPacket>();
 #if PROTOCOL_VERSION < 759 /* < 1.19 */
-        loginstart_packet->SetGameProfile(name);
+            loginstart_packet->SetGameProfile(name);
 #else
-        loginstart_packet->SetName_(name);
-        if (authentifier)
-        {
+            loginstart_packet->SetName_(name);
+            if (authentifier)
+            {
 #if PROTOCOL_VERSION < 761 /* < 1.19.3 */
-            ProfilePublicKey key;
-            key.SetTimestamp(authentifier->GetKeyTimestamp());
-            key.SetKey(Utilities::RSAToBytes(authentifier->GetPublicKey()));
-            key.SetSignature(Utilities::DecodeBase64(authentifier->GetKeySignature()));
+                ProfilePublicKey key;
+                key.SetTimestamp(authentifier->GetKeyTimestamp());
+                key.SetKey(Utilities::RSAToBytes(authentifier->GetPublicKey()));
+                key.SetSignature(Utilities::DecodeBase64(authentifier->GetKeySignature()));
 
-            loginstart_packet->SetPublicKey(key);
+                loginstart_packet->SetPublicKey(key);
 #else
-            message_sent_index = 0;
+                message_sent_index = 0;
 #endif
 #if PROTOCOL_VERSION > 759 /* > 1.19 */
-            loginstart_packet->SetProfileId(authentifier->GetPlayerUUID());
+                loginstart_packet->SetProfileId(authentifier->GetPlayerUUID());
 #endif
-        }
+            }
 #endif
-        Send(loginstart_packet);
+            Send(loginstart_packet);
+        }
+        catch (...)
+        {
+            // Make sure the processing thread is stopped before propagating the
+            // exception, otherwise destroying this partially-constructed object
+            // would destroy a joinable std::thread and call std::terminate.
+            SetState(ConnectionState::None);
+            process_condition.notify_all();
+            if (m_thread_process.joinable())
+            {
+                m_thread_process.join();
+            }
+            throw;
+        }
     }
 
     NetworkManager::NetworkManager(const ConnectionState constant_connection_state)
@@ -117,12 +164,8 @@ namespace Botcraft
 
     void NetworkManager::Stop()
     {
-        state = ConnectionState::None;
-
-        if (com)
-        {
-            com->close();
-        }
+        LOG_INFO("[NETWORK] NetworkManager::Stop() called");
+        Abort();
 
         process_condition.notify_all();
 
@@ -133,6 +176,17 @@ namespace Botcraft
         compression = -1;
 
         com.reset();
+        LOG_INFO("[NETWORK] NetworkManager::Stop() done");
+    }
+
+    void NetworkManager::Abort()
+    {
+        SetState(ConnectionState::None);
+        if (com)
+        {
+            com->close();
+        }
+        process_condition.notify_all();
     }
 
     void NetworkManager::AddHandler(Handler* h)
@@ -142,9 +196,16 @@ namespace Botcraft
 
     void NetworkManager::Send(const std::shared_ptr<Packet> packet)
     {
-        if (com)
+        const ConnectionState current_state = state.load();
+        if (current_state == ConnectionState::None)
+        {
+            return;
+        }
+
+        LOG_INFO("[SEND] " << packet->GetName() << " (0x" << std::hex << packet->GetId() << std::dec << ") [" << StateToString(current_state) << "]");
+        std::lock_guard<std::mutex> lock(mutex_send);
+        if (state.load() != ConnectionState::None && com)
         {
-            std::lock_guard<std::mutex> lock(mutex_send);
             std::vector<unsigned char> packet_data;
             packet_data.reserve(256);
             packet->Write(packet_data);
@@ -178,7 +239,7 @@ namespace Botcraft
 
     const ConnectionState NetworkManager::GetConnectionState() const
     {
-        return state;
+        return state.load();
     }
 
     const std::string& NetworkManager::GetMyName() const
@@ -307,9 +368,10 @@ namespace Botcraft
     void NetworkManager::WaitForNewPackets()
     {
         Logger::GetInstance().RegisterThread("NetworkPacketProcessing - " + name);
+        LOG_INFO("[NETWORK] Packet processing thread started");
         try
         {
-            while (state != ConnectionState::None)
+            while (state.load() != ConnectionState::None)
             {
                 {
                     std::unique_lock<std::mutex> lck(mutex_process);
@@ -372,6 +434,7 @@ namespace Botcraft
             LOG_FATAL("Unknown exception");
             throw;
         }
+        LOG_INFO("[NETWORK] Packet processing thread exiting (state=" << StateToString(state.load()) << ")");
     }
 
     void NetworkManager::ProcessPacket(const std::vector<unsigned char>& bytes)
@@ -386,10 +449,20 @@ namespace Botcraft
 
         const int packet_id = ReadData<VarInt>(packet_iterator, length);
 
-        std::shared_ptr<Packet> packet = CreateClientboundPacket(state, packet_id);
+        std::shared_ptr<Packet> packet = CreateClientboundPacket(state.load(), packet_id);
 
         if (packet != nullptr)
         {
+            // When switching backend servers through a proxy (hub -> grief via
+            // BungeeCord), the new server sends a fresh Login (Join Game) followed
+            // by a Respawn on the same Play connection. Vanilla clients process
+            // both: the Login resets the local player entity id / dimension and
+            // the Respawn unloads the old world chunks. Skipping them leaves the
+            // bot with stale state, the server rubber-bands it with Player
+            // Position corrections and kicks it after a few seconds, so any
+            // Login/Respawn in Play state is processed here.
+
+            LOG_INFO("[RECV] " << packet->GetName() << " (0x" << std::hex << packet_id << std::dec << ") [" << StateToString(state.load()) << "]");
             try
             {
                 packet->Read(packet_iterator, length);
@@ -401,9 +474,24 @@ namespace Botcraft
             }
             for (size_t i = 0; i < subscribed.size(); i++)
             {
-                packet->Dispatch(subscribed[i]);
+                try
+                {
+                    packet->Dispatch(subscribed[i]);
+                }
+                catch (const std::exception& e)
+                {
+                    LOG_ERROR("Exception while handling packet \"" << packet->GetName() << "\": " << e.what());
+                }
+                catch (...)
+                {
+                    LOG_ERROR("Unknown exception while handling packet \"" << packet->GetName() << "\"");
+                }
             }
         }
+        else
+        {
+            LOG_WARNING("[RECV] Unknown packet (0x" << std::hex << packet_id << std::dec << ") [" << StateToString(state.load()) << "]");
+        }
     }
 
     void NetworkManager::OnNewRawData(const std::vector<unsigned char>& bytes)
@@ -428,12 +516,19 @@ namespace Botcraft
 #endif
     {
 #if PROTOCOL_VERSION < 764 /* < 1.20.2 */
-        state = ConnectionState::Play;
+        SetState(ConnectionState::Play);
 #else
-        state = ConnectionState::Configuration;
+        SetState(ConnectionState::Configuration);
         std::shared_ptr<ServerboundLoginAcknowledgedPacket> login_ack_packet = std::make_shared<ServerboundLoginAcknowledgedPacket>();
         Send(login_ack_packet);
 
+        SendBrandAndClientInformation();
+#endif
+    }
+
+#if PROTOCOL_VERSION > 763 /* > 1.20.1 */
+    void NetworkManager::SendBrandAndClientInformation()
+    {
         std::shared_ptr<ServerboundCustomPayloadConfigurationPacket> custom_payload_packet = std::make_shared<ServerboundCustomPayloadConfigurationPacket>();
         custom_payload_packet->SetIdentifier("minecraft:brand");
         std::vector<unsigned char> payload;
@@ -455,8 +550,8 @@ namespace Botcraft
         client_info_packet->SetClientInformation(info);
 
         Send(client_info_packet);
-#endif
     }
+#endif
 
     void NetworkManager::Handle(ClientboundHelloPacket& packet)
     {
@@ -645,12 +740,58 @@ namespace Botcraft
 #endif
     }
 
+    namespace
+    {
+        // Vanilla client resource pack status IDs
+        constexpr int ResourcePackSuccessfullyLoaded = 0;
+        constexpr int ResourcePackAccepted = 3;
+#if PROTOCOL_VERSION > 764 /* > 1.20.2 */
+        constexpr int ResourcePackDownloaded = 4;
+#endif
+    }
+
 #if PROTOCOL_VERSION > 763 /* > 1.20.1 */
+#if PROTOCOL_VERSION == 764 /* 1.20.2 */
+    void NetworkManager::Handle(ClientboundResourcePackConfigurationPacket& packet)
+    {
+        // Accept the resource pack like the vanilla client: Accepted, then Successfully loaded
+        std::shared_ptr<ServerboundResourcePackConfigurationPacket> accepted = std::make_shared<ServerboundResourcePackConfigurationPacket>();
+        accepted->SetAction(ResourcePackAccepted);
+        Send(accepted);
+
+        std::shared_ptr<ServerboundResourcePackConfigurationPacket> loaded = std::make_shared<ServerboundResourcePackConfigurationPacket>();
+        loaded->SetAction(ResourcePackSuccessfullyLoaded);
+        Send(loaded);
+    }
+#endif
+
+#if PROTOCOL_VERSION > 764 /* > 1.20.2 */
+    void NetworkManager::Handle(ClientboundResourcePackPushConfigurationPacket& packet)
+    {
+        // Accept the resource pack like the vanilla client: Accepted, Downloaded, then Successfully loaded
+        std::shared_ptr<ServerboundResourcePackConfigurationPacket> accepted = std::make_shared<ServerboundResourcePackConfigurationPacket>();
+        accepted->SetUuid(packet.GetUuid());
+        accepted->SetAction(ResourcePackAccepted);
+        Send(accepted);
+
+        std::shared_ptr<ServerboundResourcePackConfigurationPacket> downloaded = std::make_shared<ServerboundResourcePackConfigurationPacket>();
+        downloaded->SetUuid(packet.GetUuid());
+        downloaded->SetAction(ResourcePackDownloaded);
+        Send(downloaded);
+
+        std::shared_ptr<ServerboundResourcePackConfigurationPacket> loaded = std::make_shared<ServerboundResourcePackConfigurationPacket>();
+        loaded->SetUuid(packet.GetUuid());
+        loaded->SetAction(ResourcePackSuccessfullyLoaded);
+        Send(loaded);
+    }
+#endif
+
     void NetworkManager::Handle(ClientboundFinishConfigurationPacket& packet)
     {
-        state = ConnectionState::Play;
+        // The ack is a Configuration packet, send it before switching to Play
         std::shared_ptr<ServerboundFinishConfigurationPacket> finish_config_packet = std::make_shared<ServerboundFinishConfigurationPacket>();
         Send(finish_config_packet);
+        SetState(ConnectionState::Play);
     }
 
     void NetworkManager::Handle(ClientboundKeepAliveConfigurationPacket& packet)
@@ -669,9 +810,15 @@ namespace Botcraft
 
     void NetworkManager::Handle(ClientboundStartConfigurationPacket& packet)
     {
-        state = ConnectionState::Configuration;
+        LOG_INFO("[NETWORK] Start Configuration received (server transfer / reconfiguration)");
+        // "Configuration Acknowledged" is a Play packet, send it before switching
+        // to the Configuration state.
         std::shared_ptr<ServerboundConfigurationAcknowledgedPacket> config_ack_packet = std::make_shared<ServerboundConfigurationAcknowledgedPacket>();
         Send(config_ack_packet);
+        SetState(ConnectionState::Configuration);
+
+        // Re-send the client brand and settings to the (possibly new) server
+        SendBrandAndClientInformation();
     }
 
     void NetworkManager::Handle(ClientboundChunkBatchStartPacket& packet)
@@ -690,6 +837,39 @@ namespace Botcraft
     }
 #endif
 
+#if PROTOCOL_VERSION < 765 /* < 1.20.3 */
+    void NetworkManager::Handle(ClientboundResourcePackPacket& packet)
+    {
+        // Accept the resource pack like the vanilla client: Accepted, then Successfully loaded
+        std::shared_ptr<ServerboundResourcePackPacket> accepted = std::make_shared<ServerboundResourcePackPacket>();
+        accepted->SetAction(ResourcePackAccepted);
+        Send(accepted);
+
+        std::shared_ptr<ServerboundResourcePackPacket> loaded = std::make_shared<ServerboundResourcePackPacket>();
+        loaded->SetAction(ResourcePackSuccessfullyLoaded);
+        Send(loaded);
+    }
+#else
+    void NetworkManager::Handle(ClientboundResourcePackPushPacket& packet)
+    {
+        // Accept the resource pack like the vanilla client: Accepted, Downloaded, then Successfully loaded
+        std::shared_ptr<ServerboundResourcePackPacket> accepted = std::make_shared<ServerboundResourcePackPacket>();
+        accepted->SetUuid(packet.GetUuid());
+        accepted->SetAction(ResourcePackAccepted);
+        Send(accepted);
+
+        std::shared_ptr<ServerboundResourcePackPacket> downloaded = std::make_shared<ServerboundResourcePackPacket>();
+        downloaded->SetUuid(packet.GetUuid());
+        downloaded->SetAction(ResourcePackDownloaded);
+        Send(downloaded);
+
+        std::shared_ptr<ServerboundResourcePackPacket> loaded = std::make_shared<ServerboundResourcePackPacket>();
+        loaded->SetUuid(packet.GetUuid());
+        loaded->SetAction(ResourcePackSuccessfullyLoaded);
+        Send(loaded);
+    }
+#endif
+
 #if PROTOCOL_VERSION > 765 /* > 1.20.4 */
     void NetworkManager::Handle(ProtocolCraft::ClientboundSelectKnownPacksPacket& packet)
     {
diff --git a/botcraft/src/Network/TCP_Com.cpp b/botcraft/src/Network/TCP_Com.cpp
index 00ff57c..6972dc2 100644
--- a/botcraft/src/Network/TCP_Com.cpp
+++ b/botcraft/src/Network/TCP_Com.cpp
@@ -79,6 +79,11 @@ namespace Botcraft
     }
 #endif
 
+    void TCP_Com::SetOnDisconnect(std::function<void()> callback)
+    {
+        OnDisconnect = callback;
+    }
+
     const std::string& TCP_Com::GetIp() const
     {
         return ip;
@@ -174,6 +179,10 @@ namespace Botcraft
         }
         else
         {
+            if (OnDisconnect)
+            {
+                OnDisconnect();
+            }
             do_close();
         }
     }
@@ -214,6 +223,10 @@ namespace Botcraft
         }
         else
         {
+            if (OnDisconnect)
+            {
+                OnDisconnect();
+            }
             do_close();
         }
     }
diff --git a/protocolCraft/CMakeLists.txt b/protocolCraft/CMakeLists.txt
index 8f6e82a..fcfb5b6 100644
--- a/protocolCraft/CMakeLists.txt
+++ b/protocolCraft/CMakeLists.txt
@@ -708,6 +708,7 @@ endif(MSVC)
 if (MSVC)
     target_compile_options(protocolCraft PRIVATE "/bigobj")
     set_target_properties(protocolCraft PROPERTIES MSVC_RUNTIME_LIBRARY "MultiThreaded")
+    target_compile_definitions(protocolCraft PRIVATE _ITERATOR_DEBUG_LEVEL=0)
 endif (MSVC)
 
 # Set version
-- 
2.55.0.windows.3


From 28b7d39da7024120d1c98e7e6c907d9c32d555fe Mon Sep 17 00:00:00 2001
From: Zmee4ka376 <zmee4ka376@users.noreply.github.com>
Date: Fri, 21 Aug 2026 02:14:43 +0300
Subject: [PATCH 2/3] Full state reset on proxy server switch (duplicate Login
 in Play state)

---
 .../Game/Inventory/InventoryManager.hpp       |  1 +
 botcraft/src/Game/Entities/EntityManager.cpp  |  4 +++
 .../src/Game/Inventory/InventoryManager.cpp   | 28 +++++++++++++++++++
 botcraft/src/Game/Physics/PhysicsManager.cpp  |  8 ++++++
 botcraft/src/Game/World/World.cpp             |  5 ++++
 5 files changed, 46 insertions(+)

diff --git a/botcraft/include/botcraft/Game/Inventory/InventoryManager.hpp b/botcraft/include/botcraft/Game/Inventory/InventoryManager.hpp
index 4163bc1..bad4517 100644
--- a/botcraft/include/botcraft/Game/Inventory/InventoryManager.hpp
+++ b/botcraft/include/botcraft/Game/Inventory/InventoryManager.hpp
@@ -93,6 +93,7 @@ namespace Botcraft
         void ApplyTransaction(const InventoryTransaction& transaction);
 
     private:
+        virtual void Handle(ProtocolCraft::ClientboundLoginPacket& packet) override;
         virtual void Handle(ProtocolCraft::ClientboundContainerSetSlotPacket& packet) override;
         virtual void Handle(ProtocolCraft::ClientboundContainerSetContentPacket& packet) override;
         virtual void Handle(ProtocolCraft::ClientboundOpenScreenPacket& packet) override;
diff --git a/botcraft/src/Game/Entities/EntityManager.cpp b/botcraft/src/Game/Entities/EntityManager.cpp
index dae646b..1d221c0 100644
--- a/botcraft/src/Game/Entities/EntityManager.cpp
+++ b/botcraft/src/Game/Entities/EntityManager.cpp
@@ -53,6 +53,10 @@ namespace Botcraft
         new_player->SetGameMode(static_cast<GameType>(packet.GetCommonPlayerSpawnInfo().GetGameType()));
 #endif
         std::scoped_lock<std::shared_mutex> lock(entity_manager_mutex);
+        // On a proxy server switch, a fresh Login arrives on the same
+        // connection: every entity from the previous server is gone, so
+        // clear the map before registering the new local player
+        entities.clear();
         local_player = new_player;
         entities[packet.GetPlayerId()] = local_player;
     }
diff --git a/botcraft/src/Game/Inventory/InventoryManager.cpp b/botcraft/src/Game/Inventory/InventoryManager.cpp
index 492ae2e..6b8a614 100644
--- a/botcraft/src/Game/Inventory/InventoryManager.cpp
+++ b/botcraft/src/Game/Inventory/InventoryManager.cpp
@@ -548,6 +548,34 @@ namespace Botcraft
 #endif
 
 
+    void InventoryManager::Handle(ProtocolCraft::ClientboundLoginPacket& packet)
+    {
+        std::scoped_lock<std::shared_mutex> lock(inventory_manager_mutex);
+        // On a proxy server switch, a fresh Login arrives on the same
+        // connection: every container/transaction state of the previous
+        // server is invalid, reset everything except the player inventory
+        for (auto it = inventories.begin(); it != inventories.end(); )
+        {
+            if (it->first != Window::PLAYER_INVENTORY_INDEX)
+            {
+                it = inventories.erase(it);
+            }
+            else
+            {
+                ++it;
+            }
+        }
+        cursor = Slot();
+#if PROTOCOL_VERSION < 755 /* < 1.17 */
+        pending_transactions.clear();
+        transaction_states.clear();
+#endif
+#if PROTOCOL_VERSION > 451 /* > 1.13.2 */
+        trading_container_id = -1;
+        available_trades.clear();
+#endif
+    }
+
     void InventoryManager::Handle(ClientboundContainerSetSlotPacket& packet)
     {
         if (packet.GetContainerId() == -1 && packet.GetSlot() == -1)
diff --git a/botcraft/src/Game/Physics/PhysicsManager.cpp b/botcraft/src/Game/Physics/PhysicsManager.cpp
index 5ca5748..7c948a5 100644
--- a/botcraft/src/Game/Physics/PhysicsManager.cpp
+++ b/botcraft/src/Game/Physics/PhysicsManager.cpp
@@ -110,6 +110,14 @@ namespace Botcraft
             // If we already have a player, we need to make sure we're not in the middle
             // of a physics update while swapping the player with the new one
             std::scoped_lock<std::shared_mutex> lock(old_player->entity_mutex);
+            // Drop any pending teleportation acknowledgement from the previous server
+            teleport_id = std::nullopt;
+            ticks_since_last_position_sent = 0;
+        }
+        else
+        {
+            teleport_id = std::nullopt;
+            ticks_since_last_position_sent = 0;
         }
     }
 
diff --git a/botcraft/src/Game/World/World.cpp b/botcraft/src/Game/World/World.cpp
index 3fec0c2..39fcf57 100644
--- a/botcraft/src/Game/World/World.cpp
+++ b/botcraft/src/Game/World/World.cpp
@@ -707,6 +707,11 @@ namespace Botcraft
 
     void World::Handle(ProtocolCraft::ClientboundLoginPacket& packet)
     {
+        // When switching servers through a proxy, a fresh Login arrives on the
+        // same connection: drop every chunk belonging to the previous server,
+        // otherwise stale chunks from the old world stay around
+        UnloadAllChunks(std::this_thread::get_id());
+
         std::scoped_lock<std::shared_mutex> lock(world_mutex);
 #if PROTOCOL_VERSION < 719 /* < 1.16 */
         SetCurrentDimensionImpl(static_cast<Dimension>(packet.GetDimension()));
-- 
2.55.0.windows.3


From 72b0f0c90d3e80fb81ff15187ec9486315df308e Mon Sep 17 00:00:00 2001
From: Zmee4ka376 <zmee4ka376@users.noreply.github.com>
Date: Fri, 21 Aug 2026 02:40:16 +0300
Subject: [PATCH 3/3] Port grief-server fixes: ContainerAck vanilla echo (accepted=false),
 PhysicsManager Respawn reset, Play-state send guard

---
 .../botcraft/Game/Physics/PhysicsManager.hpp  |  1 +
 botcraft/src/Game/ConnectionClient.cpp        | 10 ++++--
 botcraft/src/Game/Physics/PhysicsManager.cpp  | 28 +++++++++++++++
 botcraft/src/Network/NetworkManager.cpp       | 11 ++++++
 .../include/protocolCraft/BasePacket.hpp      |  1 +
 .../include/protocolCraft/Packet.hpp          |  3 ++
 protocolCraft/src/BasePacket.cpp              | 35 +++++++++++++++++++
 7 files changed, 86 insertions(+), 3 deletions(-)

diff --git a/botcraft/include/botcraft/Game/Physics/PhysicsManager.hpp b/botcraft/include/botcraft/Game/Physics/PhysicsManager.hpp
index 4fa7c68..a595390 100644
--- a/botcraft/include/botcraft/Game/Physics/PhysicsManager.hpp
+++ b/botcraft/include/botcraft/Game/Physics/PhysicsManager.hpp
@@ -54,6 +54,7 @@ namespace Botcraft
 
     protected:
         virtual void Handle(ProtocolCraft::ClientboundLoginPacket& packet) override;
+        virtual void Handle(ProtocolCraft::ClientboundRespawnPacket& packet) override;
         virtual void Handle(ProtocolCraft::ClientboundPlayerPositionPacket& packet) override;
 #if PROTOCOL_VERSION > 764 /* > 1.20.2 */
         virtual void Handle(ProtocolCraft::ClientboundTickingStatePacket& packet) override;
diff --git a/botcraft/src/Game/ConnectionClient.cpp b/botcraft/src/Game/ConnectionClient.cpp
index 7748dc1..9a62e96 100644
--- a/botcraft/src/Game/ConnectionClient.cpp
+++ b/botcraft/src/Game/ConnectionClient.cpp
@@ -105,14 +105,18 @@ namespace Botcraft
 #if PROTOCOL_VERSION < 755 /* < 1.17 */
     void ConnectionClient::Handle(ClientboundContainerAckPacket& packet)
     {
-        // If the transaction was not accepted, we must apologize
-        // else it's processed in InventoryManager
+        // The container ack handling (including the "acknowledge rejection"
+        // echo for transactions we actually made) lives in the
+        // InventoryManager, which knows which transaction ids are ours.
+        // Echoing here blindly would also answer the anti-cheat's probe
+        // pings (unknown/negative ids), which is what gets the bot flagged
+        // as a fake client. So this handler does nothing.
-        if (!packet.GetAccepted())
-        {
-            std::shared_ptr<ServerboundContainerAckPacket> apologize_packet = std::make_shared<ServerboundContainerAckPacket>();
-            apologize_packet->SetContainerId(packet.GetContainerId());
-            apologize_packet->SetUid(packet.GetUid());
-            apologize_packet->SetAccepted(packet.GetAccepted());
-
-            network_manager->Send(apologize_packet);
-        }
     }
 #endif
+
+    // InventoryManager.cpp: Handle(ClientboundContainerAckPacket) returns
+    // silently for unknown containers/uids (anti-cheat probes are ignored,
+    // not answered) and echoes only our own rejected transactions back with
+    // accepted=true, like the working 123321 fork.
diff --git a/botcraft/src/Game/Physics/PhysicsManager.cpp b/botcraft/src/Game/Physics/PhysicsManager.cpp
index 7c948a5..7b32578 100644
--- a/botcraft/src/Game/Physics/PhysicsManager.cpp
+++ b/botcraft/src/Game/Physics/PhysicsManager.cpp
@@ -121,6 +121,34 @@ namespace Botcraft
         }
     }
 
+    void PhysicsManager::Handle(ClientboundRespawnPacket& packet)
+    {
+        // On a proxy network a Respawn is often just a server transfer
+        // (hub <-> game server), not an actual death: the world changed,
+        // so drop any pending teleport acknowledgement from the previous
+        // server and make sure we hold the current player
+        const std::shared_ptr<LocalPlayer> new_player = entity_manager->GetLocalPlayer();
+        std::shared_ptr<LocalPlayer> old_player;
+        {
+            std::scoped_lock<std::shared_mutex> lock(player_mutex);
+            old_player = player;
+            player = new_player;
+        }
+        if (old_player != nullptr)
+        {
+            // If we already have a player, we need to make sure we're not in the middle
+            // of a physics update while resetting the teleportation state
+            std::scoped_lock<std::shared_mutex> lock(old_player->entity_mutex);
+            teleport_id = std::nullopt;
+            ticks_since_last_position_sent = 0;
+        }
+        else
+        {
+            teleport_id = std::nullopt;
+            ticks_since_last_position_sent = 0;
+        }
+    }
+
     void PhysicsManager::Handle(ClientboundPlayerPositionPacket& packet)
     {
         std::shared_ptr<LocalPlayer> player;
diff --git a/botcraft/src/Network/NetworkManager.cpp b/botcraft/src/Network/NetworkManager.cpp
index 1d2ecc9..b9fb620 100644
--- a/botcraft/src/Network/NetworkManager.cpp
+++ b/botcraft/src/Network/NetworkManager.cpp
@@ -202,6 +202,17 @@ namespace Botcraft
             return;
         }
 
+        // Never send Play packets while not in the Play state. This can happen
+        // during a server transfer (Start Configuration) when other threads
+        // (physics, user input, ...) still try to send Play packets. The server
+        // would misinterpret them (wrong packet id for the current state) and
+        // kick us with an "internal error".
+        if (packet->GetConnectionState() == ConnectionState::Play && current_state != ConnectionState::Play)
+        {
+            LOG_WARNING("[SEND] Trying to send Play packet \"" << packet->GetName() << "\" while in " << StateToString(current_state) << " state, dropping it.");
+            return;
+        }
+
         LOG_INFO("[SEND] " << packet->GetName() << " (0x" << std::hex << packet->GetId() << std::dec << ") [" << StateToString(current_state) << "]");
         std::lock_guard<std::mutex> lock(mutex_send);
         if (state.load() != ConnectionState::None && com)
diff --git a/protocolCraft/include/protocolCraft/BasePacket.hpp b/protocolCraft/include/protocolCraft/BasePacket.hpp
index e48cb75..03af602 100644
--- a/protocolCraft/include/protocolCraft/BasePacket.hpp
+++ b/protocolCraft/include/protocolCraft/BasePacket.hpp
@@ -13,6 +13,7 @@ namespace ProtocolCraft
         virtual ~BasePacket() override {};
 
         virtual int GetId() const override;
+        virtual ConnectionState GetConnectionState() const override;
         virtual std::string_view GetName() const override { return TDerived::packet_name; }
 #ifdef PROTOCOLCRAFT_DETAILED_PARSING
         virtual std::shared_ptr<Packet> CopyTypeOnly() const override { return std::make_shared<TDerived>(); }
diff --git a/protocolCraft/include/protocolCraft/Packet.hpp b/protocolCraft/include/protocolCraft/Packet.hpp
index 5fad3b3..c7bd3d2 100644
--- a/protocolCraft/include/protocolCraft/Packet.hpp
+++ b/protocolCraft/include/protocolCraft/Packet.hpp
@@ -4,6 +4,7 @@
 #include <memory>
 
 #include "protocolCraft/NetworkType.hpp"
+#include "protocolCraft/enums.hpp"
 
 namespace ProtocolCraft
 {
@@ -30,6 +31,8 @@ namespace ProtocolCraft
 
         virtual int GetId() const = 0;
 
+        virtual ConnectionState GetConnectionState() const = 0;
+
         virtual std::string_view GetName() const = 0;
 
 #ifdef PROTOCOLCRAFT_DETAILED_PARSING
diff --git a/protocolCraft/src/BasePacket.cpp b/protocolCraft/src/BasePacket.cpp
index ab2dc8a..c8095ac 100644
--- a/protocolCraft/src/BasePacket.cpp
+++ b/protocolCraft/src/BasePacket.cpp
@@ -54,6 +54,41 @@ namespace ProtocolCraft
         }
     }
 
+    template<typename TDerived>
+    ConnectionState BasePacket<TDerived>::GetConnectionState() const
+    {
+        if constexpr (Internal::tuple_contains_type<TDerived, AllClientboundPlayPackets> ||
+            Internal::tuple_contains_type<TDerived, AllServerboundPlayPackets>)
+        {
+            return ConnectionState::Play;
+        }
+        else if constexpr (Internal::tuple_contains_type<TDerived, AllClientboundStatusPackets> ||
+            Internal::tuple_contains_type<TDerived, AllServerboundStatusPackets>)
+        {
+            return ConnectionState::Status;
+        }
+        else if constexpr (Internal::tuple_contains_type<TDerived, AllClientboundLoginPackets> ||
+            Internal::tuple_contains_type<TDerived, AllServerboundLoginPackets>)
+        {
+            return ConnectionState::Login;
+        }
+        else if constexpr (Internal::tuple_contains_type<TDerived, AllServerboundHandshakingPackets>)
+        {
+            return ConnectionState::Handshake;
+        }
+#if PROTOCOL_VERSION > 763 /* > 1.20.1 */
+        else if constexpr (Internal::tuple_contains_type<TDerived, AllClientboundConfigurationPackets> ||
+            Internal::tuple_contains_type<TDerived, AllServerboundConfigurationPackets>)
+        {
+            return ConnectionState::Configuration;
+        }
+#endif
+        else
+        {
+            static_assert(Internal::dependant_false<TDerived>, "Trying to get the state of a packet which is not in protocol tuples");
+        }
+    }
+
     template <typename TDerived>
     void BasePacket<TDerived>::DispatchImpl(Handler* handler)
     {
-- 
2.55.0.windows.3

